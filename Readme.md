# Nextflow Short Reads Germline Variant Calling
This one is for short reads with genomics variant calling using Nextflow

Quick starts
```bash
pixi run nextflow run main.nf -profile docker,test -resume 
```